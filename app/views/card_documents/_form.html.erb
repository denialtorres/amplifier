<%= form_with(model: card_document, local: true) do |form| %>
  <% if card_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card_document.errors.count, "error") %> prohibited this card_document from being saved:</h2>

      <ul>
        <% card_document.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Name" %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :pdf_file, "Upload PDF" %>
    <%= form.file_field :pdf_file, accept: 'application/pdf' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
